version: 2

sources:

  - name: pruebas # name of the source 
    schema: pruebas # this is the schema our raw data lives in
    database: ALUMNO8_DEV_BRONZE_DB # this is the name of our database

    tables:

      - name: users
        columns:
          - name: Nombre
          - name: DNI
          - name: email
          - name: fecha_alta_sistema